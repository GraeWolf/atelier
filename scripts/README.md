# Helper scripts

Project automation scripts (build personal repo, invoke ISO build, common checks).

Scripts will be added as milestones need them (especially Steps 3–4). Prefer small, readable shell scripts with clear usage comments.

Do not place secrets or private keys here.
